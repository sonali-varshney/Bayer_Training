name: Sample github workflow
on:
  push:
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: first job
       uses: actions/Bayer_Training@v2
         with:
           who-to-greet: 'Mona the Octocat'
    
      
        
  
