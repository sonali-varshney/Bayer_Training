name: Sample github workflow
on:
  push:
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: Checkout
       uses: actions/checkout@v5.0.0
     - name: first job
       uses: ./
       with:
         who-to-greet: 'Sonali'
    
      
        
  
